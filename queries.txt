CREATE TABLE REGISTERED(ROLLNO INTEGER,SUBJECTID INTEGER,MARKS FLOAT);
CREATE TABLE STUDENTS(ROLLNO INTEGER, PROGRAMID INTEGER,FIRSTNAME VARCHAR(50),SECONDNAME VARCHAR(50));
CREATE TABLE COURSES(SUBJECTID INTEGER,SUBJECTNAME VARCHAR(50),CREDITS INTEGER,PROFNAME VARCHAR(50));
CREATE TABLE COURSES(SUBJECTID INTEGER,SUBJECTNAME VARCHAR(50),CREDITS INTEGER,PROFNAME VARCHAR(50));
CREATE TAB COURSES(SUBJECTID INTEGER,SUBJECTNAME VARCHAR(50),CREDITS INTEGER,PROFNAME VARCHAR(50));
SELECT * FROM COURSES;
SELECT REGISTERED.ROLLNO,MARKS,FIRSTNAME FROM REGISTERED,STUDENTS WHERE REGISTERED.ROLLNO = STUDENTS.ROLLNO AND PROGRAMID=5; 
SELECT REGISTERED.ROLLNO,MARKS,FIRSTNAME FROM REGISTERED,STUDENTS WHERE REGISTERED.ROLLNO = STUDENTS.ROLLNO AND PROGRAMID='5';
SELECT ROLLNO,PROGRAMID FROM STUDENTS WHERE FIRSTNAME LIKE 'RAMYA';
SELECT DISTINCT(ROLLNO) FROM REGISTERED WHERE MARKS > 34.0 ;
SELECT  SUBJECTID from REGISTERED GROUP BY SUBJECTID;
SELECT  SUBJECTID from REGISTERED GROUP BY SUBJECTID HAVING SUBJECTID <= 10 ORDER BY SUBJECTID;
SELECT SUBJECTID,SUBJECTS FROM COURSES;
SELECT * FROM DBS;
